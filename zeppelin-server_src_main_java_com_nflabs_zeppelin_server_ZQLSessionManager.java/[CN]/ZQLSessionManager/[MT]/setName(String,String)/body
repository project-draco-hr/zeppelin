{
  ZQLSession s=get(sessionId);
  if (s == null) {
    return null;
  }
 else {
    s.setName(name);
    try {
      persist(s);
    }
 catch (    IOException e) {
      logger.error("Can't persist session " + sessionId,e);
    }
    return s;
  }
}
