{
  Path path=getPathForSessionId(sessionId);
  try {
    ZQLSession session=load(path);
    if (session != null) {
      return session;
    }
 else {
      return null;
    }
  }
 catch (  IOException e) {
    return null;
  }
}
