{
  TreeMap<String,ZQLSession> sessions=sessionManager.list();
  return new JsonResponse<LinkedList<ZQLSession>>(Status.OK,"",new LinkedList<ZQLSession>(sessions.descendingMap().values())).build();
}
