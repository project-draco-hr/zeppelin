{
  int port=getInt(ConfVars.ZEPPELIN_WEBSOCKET_PORT);
  int serverPort=getServerPort();
  if (port < 0) {
    if (serverPort <= 0) {
      return 0;
    }
 else {
      return serverPort + 1;
    }
  }
 else {
    return port;
  }
}
