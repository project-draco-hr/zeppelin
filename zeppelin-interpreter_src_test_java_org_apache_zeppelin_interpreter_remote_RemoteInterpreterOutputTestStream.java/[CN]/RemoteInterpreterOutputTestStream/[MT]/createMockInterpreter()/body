{
  RemoteInterpreter intp=new RemoteInterpreter(new Properties(),MockInterpreterOutputStream.class.getName(),new File("../bin/interpreter.sh").getAbsolutePath(),"fake",env,10 * 1000,this);
  intpGroup.add(intp);
  intp.setInterpreterGroup(intpGroup);
  return intp;
}
