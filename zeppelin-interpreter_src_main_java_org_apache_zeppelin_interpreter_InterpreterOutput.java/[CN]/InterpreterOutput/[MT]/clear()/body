{
synchronized (outList) {
    type=InterpreterResult.Type.TEXT;
    buffer.reset();
    outList.clear();
    if (watcher != null) {
      watcher.clear();
    }
    flushListener.onUpdate(this,new byte[]{});
  }
}
