{
  InterpreterSetting setting=ZeppelinServer.notebook.getInterpreterFactory().add(tempName,"newGroup",new LinkedList<Dependency>(),new InterpreterOption(false),new Properties());
  return setting.id();
}
