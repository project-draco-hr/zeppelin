{
  getRepo(0).save(note);
  if (getRepoCount() > 1) {
    try {
      getRepo(1).save(note);
    }
 catch (    IOException e) {
      LOG.info(e.getMessage() + ": Failed to write to secondary storage");
    }
  }
}
