{
  Z z=new Q("select * from bank").pipe(new Q("select * from (${" + Q.PREV_VAR_NAME + "}) q limit 10")).pipe(new Q("create view vv as select * from ${" + Q.PREV_VAR_NAME + "}"));
  assertEquals("CREATE VIEW " + z.name() + " AS create view vv as select * from "+ z.prev().name(),z.getQuery());
}
