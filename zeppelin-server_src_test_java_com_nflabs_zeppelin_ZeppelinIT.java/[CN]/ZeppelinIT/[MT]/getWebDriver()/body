{
  if (System.getProperty("runningFromMaven") == null) {
    return new SafariDriver();
  }
 else {
    ScreenCaptureHtmlUnitDriver driver=new ScreenCaptureHtmlUnitDriver();
    driver.setJavascriptEnabled(true);
    return driver;
  }
}
