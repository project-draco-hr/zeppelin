{
  String className;
  if (uri.startsWith("jdbc:hive://")) {
    className=HIVE_SERVER;
  }
 else {
    className=HIVE_SERVER_2;
  }
  return getConf().getString("HIVE_DRIVER_CLASS","hive.driver.class",className);
}
