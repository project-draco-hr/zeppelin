{
  Note note;
synchronized (notes) {
    note=notes.remove(id);
  }
  note.getNoteReplLoader().destroyAll();
  try {
    note.unpersist();
  }
 catch (  IOException e) {
    e.printStackTrace();
  }
}
