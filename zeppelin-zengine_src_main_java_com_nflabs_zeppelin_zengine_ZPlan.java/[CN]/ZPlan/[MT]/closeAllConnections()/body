{
  for (int i=0; i < this.size(); i++) {
    Z zz=this.get(i);
    ZeppelinConnection conn=zz.getConnection();
    if (conn != null) {
      if (conn.isConnected()) {
        conn.close();
      }
    }
  }
}
