{
  ZQL zql=new ZQL("select\n*\nfrom\n,<??????> 'quote' \"doublequote\"");
  List<Z> q=zql.compile();
  assertEquals(1,q.size());
  assertEquals("select\n*\nfrom\n,<??????> 'quote' \"doublequote\"",q.get(0).getQuery());
}
