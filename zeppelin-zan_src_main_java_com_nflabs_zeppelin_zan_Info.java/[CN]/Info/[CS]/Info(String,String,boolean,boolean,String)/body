{
  this.name=name;
  this.path=path;
  this.commit=commit;
  if (installed) {
    if (updateAvailable) {
      status=Status.UPDATEAVAILABLE;
    }
 else {
      status=Status.INSTALLED;
    }
  }
 else {
    status=Status.UNINSTALLED;
  }
}
