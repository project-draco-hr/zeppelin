{
  ZQLJob s=get(jobId);
  if (s == null) {
    return null;
  }
 else {
    s.setCron(cron);
    try {
      persist(s);
      refreshQuartz(s);
    }
 catch (    IOException e) {
      logger.error("Can't persist job " + jobId,e);
    }
    return s;
  }
}
