{
  ZQLJob s=get(jobId);
  if (s == null) {
    return null;
  }
  if (s.getStatus() == Status.RUNNING) {
    return s;
  }
 else {
    s.setStatus(Status.READY);
  }
  s.setListener(this);
  scheduler.submit(s);
  return s;
}
