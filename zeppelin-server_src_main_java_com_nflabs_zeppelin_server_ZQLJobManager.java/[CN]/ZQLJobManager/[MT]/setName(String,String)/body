{
  ZQLJob s=get(jobId);
  if (s == null) {
    return null;
  }
 else {
    s.setJobName(name);
    try {
      persist(s);
    }
 catch (    IOException e) {
      logger.error("Can't persist job " + jobId,e);
    }
    return s;
  }
}
