{
synchronized (this) {
    if (conn != null) {
      try {
        conn.abort();
        return true;
      }
 catch (      ZeppelinDriverException e) {
        logger().error("Abort failure",e);
        return false;
      }
    }
 else {
      return true;
    }
  }
}
